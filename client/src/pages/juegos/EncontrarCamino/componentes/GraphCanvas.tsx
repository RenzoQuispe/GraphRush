import { GraphEdge } from "./GraphEdge"
import { GraphNode } from "./GraphNode"

export function GraphCanvas() {
    return (
        <div>
            GraphCanvas
            <GraphNode />
            <GraphEdge />
        </div>
    )
}