import type { EncontrarCaminoGameConfig } from "../types";

export const GAME_CONFIG: EncontrarCaminoGameConfig = {
    initialTime: 60,
    timeBonus: 1,
    pointsPerGraph: 100,
    comboMultiplier: 1.5,
    maxPauses: 5,
};